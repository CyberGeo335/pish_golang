# Практическое задание №8: Работа с MongoDB: подключение, создание коллекции, CRUD-операции
`ФИО`: Козин Георгий Александрович

`Группа`: ПИМО-01-25

## Цель:
* Понять базовые принципы документной БД MongoDB (документ, коллекция, BSON, _id:ObjectID);
* Научиться подключаться к MongoDB из Go с использованием официального драйвера;
* Создать коллекцию, индексы и реализовать CRUD для одной сущности (например, notes);
* Отработать фильтрацию, пагинацию, обновления (в т.ч. частичные), удаление и обработку ошибок;

---

> 💡 Important: В целях оптимизации рабочего пространства, было принято решение создать один репозиторий с дирикториями
> домашних работ.
---

## Задание:
* Понять базовые принципы документной БД MongoDB (документ, коллекция, BSON, _id:ObjectID);
* Научиться подключаться к MongoDB из Go с использованием официального драйвера;
* Создать коллекцию, индексы и реализовать CRUD для одной сущности (например, notes);
* Отработать фильтрацию, пагинацию, обновления (в т.ч. частичные), удаление и обработку ошибок;


### Описание проекта и требования:
#### Структура проекта:
```
├── prak_eight
│   ├── README.md
│   ├── assets
│   ├── cmd
│   │   └── prak_eight
│   │       └── main.go
│   ├── docker-compose.yaml
│   ├── go.mod
│   ├── go.sum
│   └── internal
│       ├── app
│       │   └── app.go
│       ├── db
│       │   └── mongo.go
│       └── notes
│           ├── handlers.go
│           ├── model.go
│           └── repo.go

```

#### Запуск проекта:
1) Клоним репозиторий:
```bash
git clone https://github.com/CyberGeo335/pish_golang.git
```
2) Проверяем что Go и Git есть:
```bash
prak_eight % go version
go version go1.23.2 darwin/arm64
prak_eight % git --version
git version 2.39.5 (Apple Git-154)
prak_eight % 
```
3) Переходим в пятую домашнюю работу:
```bash
cd prak_eight/
```
4) Для того чтобы Скрипт выполнялся к БД, в дириктории `prak_eight` у вас должен быть файл `.env`:
```bash
MONGO_URI=mongodb://root:secret@localhost:27017/?authSource=admin
MONGO_DB=pz8_mdb
HTTP_ADDR=:8080
```
5) Запуск проекта:
```bash
prak_eight % go run ./cmd/prak_eight
```
#### Проверка работоспособности:
0) Поднимем docker контейнер для MongoDB:
```bash
pish_golang % cd prak_eight/

prak_eight % docker compose up -d
```

Проверка того что контейнер поднят:
![Скриншот запуска](./assets/Снимок%20экрана%202025-11-15%20в%2015.34.01.png)

1) Проверим работоспособность через curl:
```bash
curl -X POST -H "Content-Type: application/json" -d '{"title": "First note", "content": "Hello Mongo!"}' http://localhost:8080/api/v1/notes

```
Далее получим список:
```bash
http://localhost:8080/api/v1/notes?limit=5&skip=0&q=first

```

Результат работы:

![Скриншот запуска](./assets/Снимок%20экрана%202025-11-15%20в%2015.42.09.png)




http://localhost:8080/api/v1/notes/6918625be506f7ba300e2a97






